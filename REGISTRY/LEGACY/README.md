# Legacy Registry Notice

The lowercase `registry/` directory is considered legacy.

Canonical registry path:

REGISTRY/

Do not add new files to lowercase `registry/`.
Future migration will move or remove legacy content after validation.
