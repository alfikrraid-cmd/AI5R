/**
 * AI5R Design System — typography tokens. fontFamily matches the
 * pre-existing dashboard body font (src/index.css) exactly.
 */
const typography = {
  fontFamily: "Arial, sans-serif",
  size: {
    sm: 12,
    md: 14,
    lg: 18,
    xl: 24,
  },
  weight: {
    regular: 400,
    bold: 700,
  },
};

export default typography;
